#!/usr/bin/env perl

use strict;
use lib qw(./lib);
use feature qw(say);
use Data::Dumper;
use GoatKCD;

my $beast_mode = 1;
my $file = '';
my $url = '';
my $comic_number = 0;
my $latest = 0;
my $dir = '';
our $display=0;
our $debug=0;
my $opd='';
our $output_file_name;

my $gkcd = GoatKCD->new(
	beastmode=>1,
	tmpdir=>"./tmp",
	debug=>1,
	auto_goatify=>1,
);

#my $img = $gkcd->summon_the_goatman(sprintf("/home/fatty/workspace/auto_goatkcd/originals/%s.png",$ARGV[0]))->Display();
$gkcd->summon_the_goatman($ARGV[0])->Display();

say $gkcd->panelcount;

say $gkcd->rowcount;


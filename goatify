#!/usr/bin/env perl

use Moose;
use lib qw(./lib);
use GoatKCD::CLI;
use feature qw(say);

my $cli = GoatKCD::CLI->new();
if (scalar(@ARGV)) {
	$cli->debug;
	$cli->command(@ARGV);
} else {
	$cli->main();
}
